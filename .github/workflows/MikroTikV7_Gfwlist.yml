name: MikroTikV7_Gfwlist_Scripts

on:
  workflow_dispatch:
  schedule:
    - cron: '20 20 * * *'

permissions:
  contents: write

jobs:
  update-gfwlist:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repo
        uses: actions/checkout@v4

      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.10'

      - name: Install dependencies
        run: pip install tldextract

      - name: Run script to update gfwlist domains
        run: python MikroTikV7_Gfwlist.py

      - name: Configure git
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"

      - name: Commit and push changes
        run: |
          git add gfwlist_sld.txt gfwlist7_domain.rsc
          git commit -m "Auto update GFWList domains [skip ci]" || echo "No changes to commit"
          git push https://x-access-token:${{ secrets.GITHUB_TOKEN }}@github.com/CodiFelix/MikroTikV7_Gfwlist.git HEAD:main
