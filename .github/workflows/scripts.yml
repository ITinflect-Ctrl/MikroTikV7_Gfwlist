name: Scripts

on:
  schedule:
    - cron: "0 0 * * 1"   # 每周一 0 点（UTC时间）
  workflow_dispatch:

jobs:
  run-scripts:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
      - name: Run a sample script
        run: echo "This is your weekly scheduled GitHub Actions job!"
