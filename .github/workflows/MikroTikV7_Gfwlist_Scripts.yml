name: MikroTikV7_Gfwlist_Scripts

on:
  schedule:
    - cron: "0 0 * * 1"   # 每周一 0 点（UTC时间）
  workflow_dispatch:

jobs:
  run-scripts:
    runs-on: ubuntu-latest

    steps:
      - name: 检出仓库代码
        uses: actions/checkout@v4

      - name: 赋予脚本执行权限
        run: chmod +x ./gfwlistrosv7.sh ./gfwlistdnsmasq.sh

      - name: 执行主脚本
        run: ./gfwlistrosv7.sh

      - name: 展示 Result 目录内容（可选调试用）
        run: ls -al Result || echo "Result 目录不存在"

      - name: 自动提交 Result 目录新文件到仓库
        run: |
          git config --local user.email "github-actions[bot]@users.noreply.github.com"
          git config --local user.name "github-actions[bot]"
          git add Result/
          git diff --staged --quiet || git commit -m "🤖 自动更新 IP 列表 - $(date +'%Y-%m-%d %H:%M:%S')"
          git push
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
